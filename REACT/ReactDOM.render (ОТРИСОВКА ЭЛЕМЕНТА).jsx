//# ReactDOM.render (ОТРИСОВКА ЭЛЕМЕНТА)
// Рендеринг элемента в DOM.

//# Синтаксис
ReactDOM.render(React.createElement('h1', null, 'Привет, React!'), document.getElementById('root'));
//* 1. Элемент: разметка, которую мы хотим поместить в корневой узел.
//* 2. Строка: указатель на DOM-узел, который будем использовать в качестве корневого.

//# Возвращает
//* None (undefined).
