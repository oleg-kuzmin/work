//# Преобразование (= СТРОКА)
// Явное преобразование типов - глобальный объект String превращает переданный ему аргумент в строку.

String(2);         // "2"
String(NaN);       // "NaN"
String(undefined); // "undefined"
String(null);      // "null"
String(true);      // "true"
