//# element.insertAdjacentText() (ДОБАВЛЯЕТ НОВЫЙ ТЕКСТ)
// Добавляет текст в документ и не затрагивает существующие элементы.

//# Синтаксис
//* 1. Строка - место добавления ('beforebegin' <div> 'afterbegin' <текущая разметка> 'beforeend' </div> 'afterend')
//* 2. Строка - текст
element.insertAdjacentText('beforeend', 'новый текст');

//# Возвращает
//* NONE (undefined).
