// типы 1-7 называются «примитивными», потому что их значениями могут быть только простые значения
// (будь то строка, или число, или что-то ещё).

// число            - number (для любых чисел, ограничены диапазоном ±(2 в 53 степени -1)
// большое число    - bigInt (для целых чисел произвольной длины)
// строка           - string (может содержать ноль или больше символов, нет отдельного символьного типа)
// булевый тип      - boolean (true || false)
// неизвестно       - null (для неизвестных значений)
// неопределен      - undefined (для неприсвоенных значений)
// символ           - symbol (для уникальных идентификаторов.)
// объект           - object (для более сложных структур данных.)

// typeof - определяет тип данных и возвращает значения

// TODO 1. number
// целочисленные значения
// числа с плавающей точкой
// Infinity
alert( 1 / 0 ); // Infinity
alert( Infinity ); // Infinity

// -Infinity

// NaN (вычислительная ошибка, результат неправильной или неопределённой математической операции,
// любая математическая операция с NaN возвращает NaN:)
alert( "не число" / 2 ); // NaN, такое деление является ошибкой
// ИСКЛЮЧЕНИЕ: NaN ** 0 равно 1

// TODO 2. BigInt (не поддерживается в IE)
// чтобы создать значение типа BigInt, необходимо добавить n в конец числового литерала:
// символ "n" в конце означает, что это BigInt
const bigInt = 1234567890123456789012345678901234567890n;

// TODO 3. string
// должна быть заключена в кавычки.
let str = "Привет";
let str2 = 'Одинарные кавычки тоже подойдут';
// шаблонная строка - обратные кавычки
let phrase = `Обратные кавычки позволяют встраивать переменные ${str}`;

// TODO 4. boolean
// может принимать только два значения: true (истина) и false (ложь).
let nameFieldChecked = true; // да, поле отмечено
let ageFieldChecked = false; // нет, поле не отмечено
let isGreater = 4 > 1; // true (результатом сравнения будет "да")

// TODO 5. null
// специальное значение, которое представляет собой «ничего», «пусто» или «значение неизвестно».
// используется для присвоения переменной «пустого» или «неизвестного» значения
let age = null;

// TODO 6. undefined
// означает, что «значение не было присвоено»
// если переменная объявлена, но ей не присвоено никакого значения, то её значением будет undefined
// используется для проверок, была ли переменная назначена
let age2;
console.log(age2); // undefined

// TODO 7. object
const person = {               // object
  myName: 'Oleg',              // string
  age: 100,                    // number
  isProgrammer: true,          // boolean
  cars: ['Lada', 'Porshe']     // array
}

// TODO 8. symbol
// используется для создания уникальных идентификаторов в объектах