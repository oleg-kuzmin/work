//# Math.pow() (Возведение в степень)
/* Метод возвращает основание (base), возведённое в степень (exponent).

base - основание
exponent - степень, в которую возводится основание base
*/

//# синтаксис
Math.pow(base, exponent);

//# пример
Math.pow(10, 3); // 1000
