//# .getHours() (Возвращает number - текущий час)

//# пример
//* без переменной
new Date().getHours(); // 17 (number)

//* с переменной
const currentDate = new Date();
currentDate.getHours(); // 17 (number)
