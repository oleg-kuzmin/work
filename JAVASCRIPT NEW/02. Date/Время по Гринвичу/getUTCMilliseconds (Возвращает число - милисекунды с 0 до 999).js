//# .getUTCMilliseconds() (Возвращает число - милисекунды с 0 до 999)

//* без переменной
new Date().getUTCMilliseconds(); // 123 (number)

//* с переменной
const currentDate = new Date();
currentDate.getUTCMilliseconds(); // 123 (number)
