//# .getUTCHours() (Возвращает число - час с 0 до 23)

//* без переменной
new Date().getUTCHours(); // 17 (number)

//* с переменной
const currentDate = new Date();
currentDate.getUTCHours(); // 17 (number)
