//# .getUTCFullYear () (Возвращает число - год)

//* без переменной
new Date().getUTCFullYear(); // 2023 (number)

//* с переменной
const currentDate = new Date();
currentDate.getUTCFullYear(); // 2023 (number)
