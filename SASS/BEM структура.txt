Структура файлов по бэм:
modules
	blocks
		_header.scss
		_page.scss
	variables
		color.scss
		index.scss

Компонент (БЛОК) не должен содержать margin, border без свойства box-sizing, а также не должен быть спозиционирован, не должен вырываться из потока, без размеров, padding можно
